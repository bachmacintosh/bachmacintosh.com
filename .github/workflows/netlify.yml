name: Netlify Build
on:
  schedule:
    - cron: "0 21,5 * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify Build
        run: curl -X POST ${{ secrets.NETLIFY_BUILD_HOOK }}
